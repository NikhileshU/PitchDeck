# Analytics Consolidation Decision — quality report

business-stakeholder · slate · 8 cards · 0 revision passes

## 1 · Unverified inputs

These values are placeholders. Confirm or replace them before presenting.

- `c2/c2b1` [data-provenance] — kpi uses placeholder data
- `c3/c3b1` [data-provenance] — chart uses placeholder data
- `c4/c4b1` [data-provenance] — chart uses placeholder data
- `c5/c5b1` [data-provenance] — kpi uses placeholder data
- `c6/c6b1` [data-provenance] — table uses placeholder data

## 2 · Errors

None.

## 3 · Warnings

None.

## 4 · Concerns

- `c5` — The 92% coverage claim rests on usage-log analysis that is cited but not attached — soften the claim or bring the analysis
- `deck` — No migration-risk card in the main line; the room will ask what breaks during the eight weeks before anyone reaches the appendix
- `c6` — Comparison criteria read as chosen after the decision — adding security-review status would blunt that objection

Concerns are editorial judgment calls. They are never auto-fixed.

## 5 · Tier-2 scorecard

| Dimension | Score | Note |
|---|---|---|
| storyline | 4 | Titles read as an argument: cost → duplication → fit → alternatives → ask. c8's timeline title is a statement of fact, not part of the argument — acceptable for an appendix. |
| verticalLogic | 4 | Each card's evidence backs its own title; c5's 92% claim leans on usage logs that are referenced but not shown on the card. |
| archetypeFit | 5 | Recommendation on card 2, problem → solution → ask in the prescribed order, appendix excluded from the count. |
| audienceFit | 4 | Cost, coverage, deadline and rollback are the right altitude for a budget owner; the seat-duplication chart may invite a tangent on licence hygiene. |
| density | 4 | Every card is two blocks or fewer plus a title claim; c6's four-row table is the densest card and still reads in under 30 seconds. |

Mean **4.2 / 5**, lowest **4** — gate (mean ≥ 3.5, no dimension < 3): **PASS**

## 6 · Revision log

No revision passes were needed.
